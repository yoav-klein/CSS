# Heights chain - settings heights
---

Now let's set the height of `main` to 95%. what willh happen is that the `.sections` container will overflow from the `main`.

If you set `overflow: auto` on main, then the scrolling box will be main.

If you set `overflow: auto` on `.sections`, the scrolling box will be sections. which is interesting, because I thought there's no overflow