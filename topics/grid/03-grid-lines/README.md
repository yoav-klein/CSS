# Grid lines - positioning using lines
---

We start by defining a wrapper box with `display: grid`. This makes all the children of this box a _grid items_.

By default, this grid has only one column, so the items are stacked on on the other.