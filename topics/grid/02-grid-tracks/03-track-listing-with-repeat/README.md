# Using repeat()
---

We can use the `repeat()` function for the track listing for large grids with many columns.

The `repeat()` notation can be used for a part or all the list of tracks of the grid.

