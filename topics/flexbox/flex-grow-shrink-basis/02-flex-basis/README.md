# flex-basis
---
By default, `flex-basis` is set to `auto`, which means `max-content`.
`flex-basis` takes the same values as `width` and `height` - `<length>-<percentage>`.

`flex-basis` is basically important in combination with `flex-grow` and `flex-shrink`

