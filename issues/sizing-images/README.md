# Sizing images
---

When you have an image with a different height and width, and you want to put them in a container that is square,
then you can use the `object-fit` and `object-position` CSS properties